# InfoLabel

An InfoLabel is a Label with an InfoButton at the end.

```rust demo
view! {
    <InfoLabel>
        <InfoLabelInfo slot>
            "This is example information for an InfoLabel. "
        </InfoLabelInfo>
        "Example label"
    </InfoLabel>
}
```
